# 디자인허브 기여자 파이프라인 자동화 제품 요구사항

## 1. 제품 개요

### 1.1. 무엇인가

본 제품은 미리캔버스 디자인허브의 기여자 파이프라인(업로드 → 심사 → 게재 → 정산·통보)에서 발생하는 반복적·규칙 기반의 운영 업무를 자동화하는 시스템이다. [PROBLEM.md](./PROBLEM.md)에서 정의한 세 가지 문제 — 심사 병목과 모호한 반려, 불투명한 정산과 무통보 처분, 표준 파일의 업로드 거부와 수작업 강요 — 를 해결하는 것을 목표로 한다.

### 1.2. PoC의 목적

본 프로젝트는 운영 환경에 즉시 배포하는 완제품이 아니라, **"자동화가 실제로 위 문제들을 해결할 수 있음을 임의의 환경에서 구현하고 운영하여 증명하는" 개념 증명(PoC)**이다.

PoC가 성공했다고 판단하는 기준은 다음과 같다.

1. 정의한 워크플로우가 내부 API를 호출하여 기대한 결과를 산출한다.
2. 자동화로 처리 가능하다고 판단한 항목(규칙 검증, 이상치 감지, SVG 변환)이 실제로 프로그램에 의해 처리됨을 실행 로그로 확인할 수 있다.
3. 사람이 개입해야 하는 영역(창작성·품질 판단)은 명확히 분리되어 남아 있다.

### 1.3. 사용자

| 사용자 | 역할 | 얻는 가치 |
| --- | --- | --- |
| 운영자(심사·정산 담당) | 워크플로우 실행 결과를 모니터링하고 예외를 처리 | 반복 수동 업무 감소, 지연·이상 징후의 조기 가시화 |
| 기여자 | 콘텐츠를 등록하고 수익을 정산받는 창작자 | 빠르고 구체적인 반려 사유 통보, 정산 이상 징후의 자동 고지, 불필요한 수작업 제거 |

## 2. 제품 원칙

본 제품을 설계할 때 준수해야 하는 원칙은 다음과 같다.

1. **점진적 도입**: 전체 문제를 한 번에 해결하지 않고, 효과가 크고 의존성이 낮은 문제부터 단계적으로 해결한다.
2. **사람 판단의 보존**: 심사의 창작성·품질 판단은 자동화하지 않는다. 자동화는 그 앞단의 기계적 검증과 주변의 반복 처리에 국한한다.
3. **정책의 불변**: 쿠폰 정산 제외, 고지 의무 등 약관·정책은 본 제품이 변경하지 않는다. 정책은 그대로 두고 프로세스 운영만 자동화한다.
4. **투명한 통보**: 자동화가 내린 판단(반려, 이상치 감지, 변환 실패)은 반드시 구체적인 사유와 함께 대상자에게 통보되어야 한다.
5. **안전한 폴백**: 자동화가 실패하거나 판단을 보류하는 경우, 기존의 사람 처리 흐름으로 원복할 수 있어야 한다.

## 3. 제품 범위

### 3.1. 포함하는 것

기여자 파이프라인의 세 단계에 걸친 자동화 기능을 세 개의 단계(Phase)로 나누어 제공한다.

| Phase | 대상 문제 | 핵심 기능 |
| --- | --- | --- |
| Phase 1 | 심사 병목과 모호한 반려 | 사전 규칙 검증, 심사 대기열 SLA 모니터링, 반려 사유 자동 통보 |
| Phase 2 | 불투명한 정산과 무통보 처분 | 정산 이상치 감지·고지, 정산 명세 자동 생성·배포 |
| Phase 3 | 표준 파일의 업로드 거부 | SVG 자동 변환과 렌더 비교 검증 |

각 Phase는 독립적으로 완성되어 가치를 제공하며, 이전 Phase 완료를 전제로 하지 않는다.

### 3.2. 포함하지 않는 것

다음은 본 제품의 범위에 포함하지 않으며, 명시적으로 배제한다.

- 쿠폰 사용분의 정산 포함 여부 등 약관·정책의 변경
- 창작성·품질에 대한 자동 심사 (AI 심사 포함)
- 제출 한도, 로열티 비율 등 운영 정책 값의 변경
- 실제 미리디 내부 시스템과의 연동 (내부 API에 접근할 수 없으므로 API의 존재를 가정한다)
- 운영 환경(클라우드, 모니터링 시스템, CI/CD)에 대한 설계

## 4. 설계 결정

### 4.1. 오케스트레이션 도구: n8n

워크플로우 오케스트레이션은 n8n을 사용한다. n8n은 미리디가 실제 업무 자동화에 사용하는 도구로, 스케줄 실행·웹훅 수신·HTTP 요청·코드 실행·알림 연동을 시각적으로 구성할 수 있어 반복 업무 자동화에 적합하다.

### 4.2. 내부 API의 존재 가정

본 제품은 미리디의 실제 내부 API를 호출할 수 없다. 따라서 필요한 내부 API가 존재한다고 **가정**하고, 그 계약(입력·출력)을 정의한다. PoC에서는 이 계약을 모사하는 스텁 서버를 두어 워크플로우를 검증하며, 실제 내부 API가 제공될 경우 계약만 맞추면 연동이 가능하도록 설계한다.

가정하는 내부 API의 영역은 다음과 같다.

| 영역 | 설명 |
| --- | --- |
| 콘텐츠 제출 | 기여자의 콘텐츠 제출 이벤트 수신, 제출물 조회 |
| 심사 | 심사 대기열 조회, 심사 결정(승인/반려) 처리 |
| 통보 | 기여자에게 알림 발송 |
| 정산 | 정산 데이터 조회 |

### 4.3. 로직과 오케스트레이션의 분리

규칙 검증과 이상치 판정처럼 여러 워크플로우에서 재사용할 수 있는 결정적 로직은 독립적으로 호출 가능한 스텁 서버 기능으로 분리한다. SVG 변환은 Phase 3 워크플로우의 Code 노드에서 직접 실행해 변환 과정과 결과를 하나의 실행 로그로 확인한다.

## 5. 기능 요구사항

### 5.1. Phase 1: 심사 파이프라인 자동화

#### 5.1.1. 사전 규칙 검증

- **목적**: 사람 심사로 넘어가기 전에 기계적으로 판정 가능한 위반을 차단하여 심사 큐 유입을 줄이고, 기여자에게 즉각적인 피드백을 제공한다.
- **트리거**: 콘텐츠 제출 이벤트 수신.
- **처리**: 제출물에 대해 다음 규칙을 순서대로 검증한다.

| 규칙 | 판정 기준 |
| --- | --- |
| 파일 규격 | 콘텐츠 유형별 해상도·픽셀·용량·포맷 기준 충족 여부 |
| 금지 속성 | SVG 내 `fill-rule="evenodd"`, `stroke` 등 차단 속성 포함 여부 |
| 요소 개수 | SVG 내부 디자인 요소 개수 상한 초과 여부 |
| 여백 비율 | 피사체 대비 대지(artboard) 여백 비율이 기준을 초과하는지 여부 |

- **산출물**: 규칙을 위반한 제출물은 심사 대기열에 들어가지 않고, 위반한 규칙의 구체적인 명칭과 수정 안내를 포함한 반려 결과를 즉시 생성한다. 모든 규칙을 통과한 제출물만 심사 대기열로 전달한다.
- **수용 기준**:
  - 규칙 위반 제출물은 사람 개입 없이 반려 결과가 생성된다.
  - 각 반려 결과에는 위반한 규칙명이 명시된다.
  - 통과한 제출물만 심사 대기열에 반영된다.

#### 5.1.2. 심사 대기열 SLA 모니터링

- **목적**: 심사 소요 시간이 안내 기간을 초과하는 상황을 조기에 감지하여 병목 장기화를 방지한다.
- **트리거**: 정기 스케줄 실행.
- **처리**: 심사 대기열을 조회하여 각 제출물의 대기 일수와 대기열 전체 물량을 집계하고, 사전에 정의한 임계값과 비교한다.

| 지표 | 임계값 예시 |
| --- | --- |
| 최장 대기 일수 | 안내 기간 초과 시 경보 |
| 대기열 물량 | 기준치 초과 시 경보 |

- **산출물**: 임계값을 초과하면 운영자에게 경보를 발송한다.
- **수용 기준**:
  - 임계값 초과 시 경보가 발송된다.
  - 정상 범위에서는 불필요한 경보가 발생하지 않는다.

#### 5.1.3. 반려 사유 자동 통보

- **목적**: 반려 사유를 일관되고 구체적인 형태로 기여자에게 전달하여 모호한 통보로 인한 혼란을 제거한다.
- **트리거**: 심사 결정(반려) 이벤트 수신.
- **처리**: 반려 결과에 포함된 위반 사유를 기반으로, 사전 정의된 템플릿을 이용해 구체적인 사유 문구를 생성하고 알림을 발송한다. 사전 검증에서 차단된 건과 사람 심사에서 반려된 건 모두 동일한 통보 체계를 사용한다.
- **산출물**: 기여자에게 전달되는 반려 알림. 사유 문구에는 위반한 규칙의 명칭이 포함된다.
- **수용 기준**:
  - 동일한 사유에 대해 항상 동일한 문구 템플릿이 적용된다.
  - 알림에는 기여자가 수정 방향을 알 수 있을 만큼의 구체적인 사유가 포함된다.

### 5.2. Phase 2: 정산·통보 자동화

#### 5.2.1. 정산 이상치 감지·고지

- **목적**: 정산 데이터에서 이상 징후를 규칙으로 감지하여, 기여자가 이를 직접 발견해야 했던 인지 지연을 제거한다.
- **트리거**: 정산 데이터 확정 후 정기 스케줄 실행.
- **처리**: 정산 데이터를 수집하여 다음 규칙으로 이상 징후를 판정한다.

| 규칙 | 판정 기준 |
| --- | --- |
| 수익 급감 | 이전 주기 대비 사용량은 유사하거나 증가했는데 수익이 기준 이상 급감한 경우 |
| 0원 급변 | 기존에 수익이 발생하던 콘텐츠가 갑자기 0원으로 집계된 경우 |
| 비공개 처리 | 기여자의 콘텐츠가 비공개 처리된 사실이 감지된 경우 |

- **산출물**: 이상 징후가 감지된 기여자에게 그 내역과 함께 자동 고지한다.
- **수용 기준**:
  - 정의한 규칙에 해당하는 이상 징후가 자동으로 감지된다.
  - 감지 결과가 해당 기여자에게 고지된다.
  - 정상 데이터에 대해서는 불필요한 고지가 발생하지 않는다.

#### 5.2.2. 정산 명세 자동 생성·배포

- **목적**: 월 합계·사용 횟수 수준에 그치는 정산 내역을 건별 상세 명세로 제공하여 정산의 투명성을 높인다.
- **트리거**: 정산 확정 후 정기 스케줄 실행.
- **처리**: 정산 데이터를 기반으로 기여자별 상세 명세를 생성하고 배포한다.
- **산출물**: 기여자가 자신의 수익 산출 근거를 확인할 수 있는 건별 상세 명세.
- **수용 기준**:
  - 명세가 자동으로 생성되고 배포된다.
  - 명세에 건별 사용 내역과 산출 금액이 포함된다.

### 5.3. Phase 3: SVG 자동 변환

#### 5.3.1. SVG 자동 변환 및 렌더 비교 검증

- **목적**: 표준 도구에서 생성되는 `fill-rule="evenodd"` 속성으로 인한 업로드 거부를 제거하여, 기여자의 수작업을 없앤다.
- **트리거**: 콘텐츠 제출 이벤트 수신(SVG 파일).
- **처리**: 다음 단계를 수행한다.

1. SVG 파일에서 `fill-rule="evenodd"` 속성을 감지한다.
2. 감지된 경우, 경로를 변환하여 채우기 규칙을 `nonzero`로 바꾼다.
3. 변환 전과 후의 렌더링 결과를 비교하여 형태 무결성을 검증한다.
4. 검증을 통과한 파일만 심사 대기열로 전달한다. 변환 또는 검증에 실패한 경우에는 구체적인 사유를 기여자에게 회신하고 사람 처리로 이관한다.

- **산출물**: 변환·검증을 통과한 SVG 파일, 또는 실패 사유 통보.
- **수용 기준**:
  - evenodd가 포함된 SVG가 사람 수작업 없이 변환된다.
  - 변환으로 인한 형태 왜곡이 렌더 비교를 통해 검출된다.
  - 실패 시 기여자에게 구체적인 사유가 전달되고, 처리 흐름이 중단되지 않는다.

## 6. 비기능 요구사항

| 항목 | 요구사항 |
| --- | --- |
| 안전한 폴백 | 자동화가 판단을 보류하거나 실패하면 기존의 사람 처리 흐름으로 원복할 수 있어야 한다. 자동화가 사람 처리 경로를 막지 않는다. |
| 멱등성 | 동일한 입력에 대해 워크플로우가 반복 실행되더라도 중복 통보·중복 처리 같은 부작용이 발생하지 않아야 한다. |
| 감사 추적 | 자동화가 내린 판단(차단, 경보, 고지, 변환)의 실행 이력이 남아 사후 확인이 가능해야 한다. |
| 운영자 가시성 | 경보·실패·예외는 운영자가 즉시 인지할 수 있도록 알림으로 전달되어야 한다. |

## 7. 성공 지표

본 제품의 효과를 측정하는 지표는 다음과 같다.

| 지표 | 측정 방법 | 기대 방향 |
| --- | --- | --- |
| 사전 검증 처리 시간 | 제출 이벤트 수신부터 규칙 검증 결과 생성까지의 시간 | 수 분 이내 |
| 반려 통보 구체성 | 반려 결과에 규칙명이 포함된 비율 | 100% |
| 심사 큐 유입 감소 | 자동 검증으로 차단된 제출물의 비율 | 기준 대비 감소 |
| 지연 감지 시점 | 안내 기간 초과가 감지된 시점의 대기 일수 | 임계값 부근 |
| 이상 징후 인지 지연 | 이상 징후 발생부터 고지까지의 시간 | 당일 |
| SVG 수작업 건수 | evenodd로 인한 수동 수정 건수 | 0건 |

## 8. 로드맵

| Phase | 범위 | 완료 기준 | 우선순위 근거 |
| --- | --- | --- | --- |
| Phase 1 | 심사 파이프라인 자동화 | 사전 검증·SLA 모니터링·반려 통보 워크플로우가 스텁 API로 검증됨 | 불만 빈도가 가장 높고, 외부 의존 없이 시작 가능 |
| Phase 2 | 정산·통보 자동화 | 이상치 감지·명세 배포 워크플로우가 스텁 API로 검증됨 | 정산 오류의 선례(3개월 미발견)가 있어 리스크가 큼 |
| Phase 3 | SVG 자동 변환 | Code 노드의 변환·렌더 비교 검증이 워크플로우 실행 로그로 검증됨 | 결정적 연산으로 자동화 가능성이 가장 명확하나 변환 로직 개발 비용이 있음 |
